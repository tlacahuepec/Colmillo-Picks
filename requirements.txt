# Runtime dependencies for the Colmillo-Picks API and UI services.
# Pin minimum compatible versions; allow patch/minor upgrades during deploy.

# API
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.6,<3.0
SQLAlchemy>=2.0,<3.0

# UI
streamlit>=1.32,<2.0
httpx>=0.27,<1.0

# Pipeline (already used by skills/soccer-prop-picks)
jsonschema>=4.21,<5.0
